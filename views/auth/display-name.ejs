<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>نام نمایشی - حیران</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('../partials/header') %>

  <main class="auth-page">
    <section class="auth-card">
      <h2 class="auth-card__title">
        <% if (hasDisplayName) { %>
          تغییر نام نمایشی
        <% } else { %>
          انتخاب نام نمایشی
        <% } %>
      </h2>
      <p class="auth-card__subtitle">
        نامی را انتخاب کن که در کنار نوشته‌هایت نمایش داده شود.
      </p>

      <% if (errors && errors.length > 0) { %>
        <ul class="auth-card__errors">
          <% errors.forEach(error => { %>
            <li><%= error %></li>
          <% }); %>
        </ul>
      <% } %>

      <form method="POST" action="/profile/display-name" class="auth-form">
        <label class="auth-form__label" for="displayNameInput">نام نمایشی</label>
        <input
          id="displayNameInput"
          name="displayName"
          type="text"
          class="auth-form__input"
          value="<%= values && values.displayName ? values.displayName : '' %>"
          placeholder="بهتر است فارسی باشد"
          required
          maxlength="40"
          minlength="2"
        />
        <button type="submit" class="btn btn--primary auth-form__submit">
          ذخیره
        </button>
      </form>

      <% if (hasDisplayName) { %>
        <form class="auth-form__logout" action="/auth/logout" method="POST" style="margin-top: 24px; padding-top: 24px; border-top: 1px solid color-mix(in srgb, var(--theme-text) 15%, transparent);">
          <button type="submit" class="btn btn--ghost" style="width: 100%;">
            خروج
          </button>
        </form>
      <% } %>
    </section>
  </main>

  <%- include('../partials/footer') %>

  <script src="/client.js"></script>
</body>
</html>


