<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('partials/header', { includePwaMeta: true }) %>
  <title><%= book.title %> - <%= poet.name %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('partials/header') %>

  <main>
    <nav class="breadcrumb">
      <a href="/" class="mobile-back-btn" aria-label="بازگشت">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="m8 6 6 6-6 6"/>
        </svg>
      </a>
      <a href="/">خانه</a>
      <span>←</span>
      <span><%= poet.name %></span>
      <span>←</span>
      <span><%= book.title %></span>
    </nav>

    <section class="book" data-poet-id="<%= poet.id %>" data-book-id="<%= book.id %>">
      <h2><%= book.title %></h2>
      <p class="poet">از <%= poet.name %></p>

      <div class="book-actions">
        <button class="random-section-btn" id="random-section-btn"
                data-section-ids='<%= JSON.stringify(book.sections.map(s => s.id)) %>'
                data-poet-id="<%= poet.id %>"
                data-book-id="<%= book.id %>">
          <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="random-section-btn__icon">
            <rect width="12" height="12" x="2" y="10" rx="2" ry="2"/>
            <path d="m17.92 14 3.5-3.5a2.24 2.24 0 0 0 0-3l-5-4.92a2.24 2.24 0 0 0-3 0L10 6"/>
            <path d="M6 18h.01"/>
            <path d="M10 14h.01"/>
            <path d="M15 6h.01"/>
            <path d="M18 9h.01"/>
          </svg>
          <span class="random-section-btn__text">فال شما</span>
        </button>
      </div>

      <ul class="section-list" id="section-list" data-total-sections="<%= totalSections %>" data-current-page="1">
        <% sections.forEach(section => { %>
          <li class="section-item">
            <a href="/<%= poet.id %>/<%= book.id %>/<%= section.id %>">
              <span><%= section.title %></span>
            </a>
          </li>
        <% }); %>
      </ul>
      
      <div class="section-loader" id="section-loader" style="<%= totalSections <= sections.length ? 'display: none;' : '' %>">
        <span class="section-loader__text">در حال بارگذاری...</span>
        <span class="section-loader__progress">
          <span id="loaded-count"><%= sections.length %></span> از <%= totalSections.toLocaleString('fa-IR') %>
        </span>
      </div>
    </section>
  </main>

  <%- include('partials/footer') %>

  <script src="/client.js"></script>
</body>
</html>
