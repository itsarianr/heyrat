<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= section.title %> - حیران</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <%- include('partials/header') %>

  <main>
    <nav class="breadcrumb">
      <a href="/">خانه</a>
      <span>←</span>
      <span><%= poet.name %></span>
      <span>←</span>
      <span><%= book.title %></span>
    </nav>

    <article class="poem">
      <h2><%= section.title %></h2>

      <div class="screenshot-controls">
        <button id="start-selection">ساخت تصویر شعر</button>
        <button id="finish-selection" style="display:none;">پایان انتخاب</button>
      </div>

      <p id="selection-hint" class="selection-hint" style="display:none;">
        ابیاتی که می‌خواهید را انتخاب کنید
      </p>

      <div class="couplets">
        <% poem.couplets.forEach((couplet, index) => { %>
          <div class="couplet" data-couplet-id="<%= poet.id %>-<%= book.id %>-<%= section.id %>-<%= poem.id %>-<%= index %>">
            <button class="favorite-btn" data-couplet-id="<%= poet.id %>-<%= book.id %>-<%= section.id %>-<%= poem.id %>-<%= index %>" aria-label="افزودن به علاقه‌مندی‌ها">
              <svg class="star-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path fill="none" d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z" />
              </svg>
            </button>
            <p class="verse verse-first"><%= couplet[0] %></p>
            <p class="verse verse-second"><%= couplet[1] %></p>
          </div>
        <% }); %>
      </div>
    </article>
  </main>

  <%- include('partials/footer') %>

  <script>
    window.POEM_DATA = {
      poetId: '<%= poet.id %>',
      poetName: '<%= poet.name %>',
      bookId: '<%= book.id %>',
      bookTitle: '<%= book.title %>',
      sectionId: '<%= section.id %>',
      poemId: '<%= poem.id %>'
    };
  </script>
  <script src="https://cdn.jsdelivr.net/npm/html2canvas@1.4.1/dist/html2canvas.min.js"></script>
  <script src="/client.js"></script>
</body>
</html>

