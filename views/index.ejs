<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>حیران - شعر پارسی</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('partials/header') %>

  <main>
    <section class="feed-cta">
      <div class="feed-cta__text">
        <h2 class="feed-cta__title">فید نوشته‌ها</h2>
        <p class="feed-cta__subtitle">
          نوشته‌های عاشقان شعر درباره ابیات محبوب‌شان را دنبال کنید.
        </p>
      </div>
      <a class="btn btn--primary feed-cta__button" href="/feed">مشاهده فید</a>
    </section>

    <% data.poets.forEach(poet => { %>
      <section class="poet-section">
        <h2 class="poet-name"><%= poet.name %></h2>
        <div class="book-cards">
          <% poet.books.forEach(book => { %>
            <a class="book-card" href="/<%= poet.id %>/<%= book.id %>">
              <h3 class="book-card__title"><%= book.title %></h3>
              <p class="book-card__meta"><%= book.sections.length.toLocaleString('fa-IR') %> بخش</p>
            </a>
          <% }); %>
        </div>
      </section>
    <% }); %>
  </main>

  <%- include('partials/footer') %>

  <script src="/client.js"></script>
</body>
</html>

